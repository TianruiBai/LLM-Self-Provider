id: "qwen/qwen3.5-122b-a10b"
kind: chat
# 122B MoE (10B active) — MXFP4 quant from unsloth.
# https://huggingface.co/unsloth/Qwen3.5-122B-A10B-GGUF
#
# This repo is distributed as 3 GGUF shards. Point llama.cpp at shard 1 and
# keep all 3 shard files in the same folder; it will discover the siblings.
#
# At ~76 GiB the weights won't fit on CUDA1 alone; rely on `--cpu-moe`
# to keep experts in CPU RAM and stream the dense path on the GPU.
#
# No `download:` block here on purpose: `/admin/fetch-model` currently
# supports downloading a single GGUF file, while this model requires all 3
# shards to be present together.
path: "C:/Users/Tass/.lmstudio/models/unsloth/Qwen3.5-122B-A10B-GGUF/Qwen3.5-122B-A10B-MXFP4_MOE-00001-of-00003.gguf"
args:
  - "--ctx-size"
  - "128000"
  - "--device"
  - "CUDA1"
  - "--split-mode"
  - "none"
  - "--n-gpu-layers"
  - "999"
  - "--cpu-moe"
  - "--cache-type-k"
  - "q8_0"
  - "--cache-type-v"
  - "q8_0"
  - "--batch-size"
  - "256"
  - "--ubatch-size"
  - "256"
  - "--parallel"
  - "1"
  - "--no-warmup"
